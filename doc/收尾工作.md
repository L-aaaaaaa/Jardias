

国际化：README、CLI中英切换，系统提示词、论文
⑤ 补齐小问题  



**🔴 必须做（阻塞开源）**

| #    | 问题                                | 说明                                                         |
| ---- | ----------------------------------- | ------------------------------------------------------------ |
| 1    | `meta/` → 根目录部署                | 把 LICENSE、NOTICE、CHANGELOG.md、TRADEMARK 复制/链接到仓库根目录；`.github/CONTRIBUTING.md` 从 `meta/CONTRIBUTING` 复制；`library/LICENSE` 从 `meta/LICENSE-PAPERS` 复制 |
| 2    | `character_data/` 加入 `.gitignore` | 当前 18 个测试角色目录包含真实对话历史（最大 124KB），必须从仓库排除，只保留一个示例配置模板 |
| 3    | README 缺失链接处理                 | 14 个引用文件不存在（12 篇 library/ 理论文档 + 2 篇 doc/ 计划文档），要么补内容要么删链接 |
| 4    | 提交所有未追踪文件 + 清理脏工作树   | `.env.example`、`.github/`、`AUTHORS`、`CODE_OF_CONDUCT.md`、`SECURITY.md`、`doc/`、`library/` 全部是 `??` 状态，16 个文件有未暂存修改 |

**🟡 应该做（影响体验）**

| #    | 问题                      | 说明                                                         |
| ---- | ------------------------- | ------------------------------------------------------------ |
| 5    | 补 `pyproject.toml`       | 无标准 Python 打包配置，用户无法 `pip install .`             |
| 8    | 清理临时文件              | 删除 `手动保存的终端输出-第2次串联.txt`；修正 `test_app.ps1` 硬编码 Python 路径 |

**🟢 锦上添花（可延后）**

| #    | 问题                 | 说明                       |
| ---- | -------------------- | -------------------------- |
| 10   | GitHub Actions CI    | 自动跑测试 + lint          |
| 11   | CHANGELOG 补历史版本 | 目前只有 `[Unreleased]` 段 |
| 12   | CLA 模板替换         | 占位 → 正式 Apache CLA PDF |
| 13   | Git tag 打版本       | `v0.1.0` 之类的            |

总共 14 项，前 9 项建议在开源前搞定，后 5 项可以发版后慢慢补。

---