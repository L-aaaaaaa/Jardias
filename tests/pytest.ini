[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q --strict-markers
markers =
    slow: 长时间运行的集成测试
    integration: 需要网络或 LLM API
filterwarnings =
    ignore::DeprecationWarning
